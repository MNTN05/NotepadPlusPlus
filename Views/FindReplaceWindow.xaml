<Window x:Class="NotepadPlusPlus.Views.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find / Replace"
        Width="460" Height="230"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Find:" VerticalAlignment="Center" Margin="0,4"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,4"/>
        <Button Grid.Row="0" Grid.Column="3" Content="Find Next"
                Command="{Binding FindNextCommand}" Margin="0,4"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Replace:" VerticalAlignment="Center" Margin="0,4"
                   Visibility="{Binding IsReplaceMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding ReplaceText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,4"
                 Visibility="{Binding IsReplaceMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <Button Grid.Row="1" Grid.Column="3" Content="Replace"
                Command="{Binding ReplaceCommand}" Margin="0,4"
                Visibility="{Binding IsReplaceMode, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <Button Grid.Row="2" Grid.Column="3" Content="Find Prev"
                Command="{Binding FindPreviousCommand}" Margin="0,4"/>

        <Button Grid.Row="3" Grid.Column="3" Content="Replace All"
                Command="{Binding ReplaceAllCommand}" Margin="0,4"
                Visibility="{Binding IsReplaceMode, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                   Text="{Binding StatusMessage}"
                   Foreground="#888" FontStyle="Italic"
                   VerticalAlignment="Bottom" Margin="0,4"/>

        <Button Grid.Row="4" Grid.Column="3" Content="Close"
                Click="Close_Click" VerticalAlignment="Bottom" Margin="0,4"/>
    </Grid>
</Window>